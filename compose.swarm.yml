# Auto-generated by spawn.sh
# 1 instance(s), each adds to 2 panel(s)
services:

  kiro-worker-1:
    build: .
    container_name: kiro-worker-1
    volumes:
      - ./output:/app/output
      - ./panels.json:/app/panels.json:ro
    environment:
      - PANELS=/app/panels.json
      - COUNT=99999
      - DOMAIN=fake.legal
      - COUNTRY=us
      - NO_PROXY=1
      - MAIL_PROVIDER=fake_legal
    restart: unless-stopped
